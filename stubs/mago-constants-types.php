<?php

declare(strict_types=1);

// optional, host-application-defined constants.

/**
 * Optional cache directory path. Defined by the host application, not the library.
 * Typed `mixed` because the value is entirely user-controlled and must be
 * validated (e.g. is_string()) before use.
 *
 * @var mixed
 */
const K_PATH_CACHE = null;

